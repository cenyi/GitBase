/** 
 * @type {import('next').NextConfig} 
 * 定义一个常量 nextConfig，用于存储 Next.js 应用的配置对象
 */
const nextConfig = {};

/**
 * 默认导出 nextConfig 对象，作为 Next.js 应用的配置
 * 这个配置对象将被 Next.js 框架用于构建和运行应用
 */
export default nextConfig;
